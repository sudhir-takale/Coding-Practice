# # def sq(num):
# #     return num*num

# num =[2,5,8,3,]
# # square-list(map(sq,num)) #--> this by simple function
# square=list(map(lambda x:x*x,num)) # -->this is the map function
# print(square)



# list=[for and ranger we are going to use the name of the following number given by the same things now  we cna also tell the programmmer given by the same things can we learn  the iswe can also try to  make things given bythe name of the programmer can we runteh givne ]