// To take the input in js from user we use the prompt which helps to take input from the user 

// We use let keyword to declare the variables in JS

let c = prompt("Enter the value of the input");
console.log(c );